import { styled } from 'styled-components';

export const BreadcrumbContainer = styled.div`
	display: flex;
	align-items: center;
	gap: 0px 8px;

	.breadcrumb-text {
		font-size: 12px;
		line-height: 1;
	}

	.slash {
		display: inline-block;
		margin-left: 8px;
		font-size: 12px;
	}
`;
