import styled from 'styled-components';

export const SelectBoxContainer = styled.div`
	display: flex;
	flex-direction: column;
	gap: 4px 0px;

	.label-container {
		font-size: 12px;
	}
	.select-input {
		width: 200px;
		height: 32px;
		font-size: 12px;
	}
`;
